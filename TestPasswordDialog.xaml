<Window x:Class="ZipCrackerUI.TestPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Password"
        Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a2e"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🧪 Test a Specific Password"
                   FontSize="18" FontWeight="Bold"
                   Foreground="#e94560" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Enter password to test:"
                   Foreground="#eaeaea" Margin="0,0,0,5"/>

        <TextBox x:Name="txtPassword" Grid.Row="2"
                 Background="#2a2a4a" Foreground="#eaeaea"
                 BorderBrush="#3a3a5a" Padding="10,8"
                 FontSize="14" FontFamily="Consolas"/>

        <Border Grid.Row="3" Background="#0d0d1a" CornerRadius="5"
                Margin="0,15,0,15" Padding="10">
            <TextBlock x:Name="lblResult" Text="Enter a password and click Test"
                       Foreground="#888" TextWrapping="Wrap"/>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnTest" Content="🔍 Test Password"
                    Background="#e94560" Foreground="White"
                    Padding="15,8" Margin="0,0,10,0"
                    Click="BtnTest_Click"/>
            <Button x:Name="btnClose" Content="Close"
                    Background="#3a3a5a" Foreground="White"
                    Padding="15,8"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
